# ── 构建产物 ──
# build/ 里有打包好的 exe（8 MB）、离屏截图、临时测试目录、从录屏抽的帧。
# 全部是"跑一遍就有"的东西，不入库。
/build/

# ── 运行数据 ──
# out/ 是**采集出来的数据**（快照 / 报告 / 导出 / 本地索引 / 界面偏好）。
# 它是"用户的数据"，不是"项目的源码"：
#   ① 会一直长（每扫一轮就多几份）；
#   ② 内容是第三方站点的榜单数据，不该随源码一起分发。
# 跑一次 `dart run bin/scan.dart sweep` 就会重新生成。
/out/

# ── 生成的网页 ──
# 单文件网页（数据全内嵌，约 1 MB）由 `开始.cmd` 选 3 或
# `dart run bin/page.dart` 生成，属于产物。
/榜单页.html

# ── 工具链缓存 ──
.dart_tool/
.packages
pubspec.lock.bak

# ── 编辑器 / 系统 ──
.idea/
.vscode/
*.iml
Thumbs.db
Desktop.ini
*.swp

# ── 本机存档 ──
# 逐轮改动说明 / 审查报告 / 带完整历史的 README —— 这些是**本机留档**，
# 不进公开仓库（公开面只保留最终版本的使用方法与功能描述）。
/.archive/

# ── 本机环境相关 ──
# 这里记着本机的 Dart SDK 绝对路径、排查过程等，属于"个人工作笔记"，
# 不该出现在公开仓库里。
.workbuddy-ai/

# ── 一次性探针输出 ──
# 开发时把命令输出重定向成的临时文件（`_xx.txt`）与调 API 时落盘的响应
# （`_xx.json`），都是排查过程的中间物。
/_*.txt
/_*.json
/_*.html
/_*.png
